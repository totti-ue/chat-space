-# メイン画面左側全体を指定
.side-bar
  -# 上：ユーザー名表示
  .side-bar__top
    .user-box
      .user-name
        = current_user.name
      .edit-setting-icons
        = link_to new_group_path, class: "edit-setting-icons__btn" do
          = icon('fa', 'edit')
        = link_to edit_user_path(current_user.id), class: "edit-setting-icons__btn" do
          = icon('fa', 'cog')
  -# 下：グループリスト表示
  .side-bar__group-lists
    - current_user.groups.each do |group|
      .a_group-list
        = link_to "#" do
          .group-name
            = group.name
          .message-notice
            まだメッセージはありません